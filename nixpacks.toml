[phases.setup]
aptPkgs = ['proj-bin', 'libproj-dev', 'proj-data']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[start]
cmd = 'gunicorn app:app --bind 0.0.0.0:$PORT'